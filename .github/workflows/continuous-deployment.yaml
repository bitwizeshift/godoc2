name: Continuous Integration

on:
  workflow_call:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy-docs:
    name: Deploy Docs
    uses: ./.github/workflows/godoc-deploy.yaml
